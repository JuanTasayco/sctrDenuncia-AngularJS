var express = require('express');
var router = express.Router();

/* GET home page. */
//router.get('/greet', function(req, res, next) {
//  res.render('/greet', { title: 'Express' });
//});

module.exports = router;