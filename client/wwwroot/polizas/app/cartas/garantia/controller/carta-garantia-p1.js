(function($root, deps, action) {
	define(deps, action);
})(this, ['angular', 'constants', 'helper'], function(angular, constants, helper) {
	angular.module('appCartas').controller('cartaGarantiaS1', ['$scope', '$state','$timeout', '$window', function($scope, $state, $window){

	}]);
});
