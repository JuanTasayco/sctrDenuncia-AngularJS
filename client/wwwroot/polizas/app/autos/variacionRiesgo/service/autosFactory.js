define(['angular', 'constants', 'wrap_gaia'], function(angular, cons)
{
    var appAutos = angular.module("appAutos");

    appAutos.factory('autosFactory',['oimHttpSrv', '$q', '$http',  function(oimHttpSrv, $q, $http)//, $http, $q, $window)
    {
        return {

        }
    }]);
});
